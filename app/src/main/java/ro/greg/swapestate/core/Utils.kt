package ro.greg.swapestate.core

import android.util.Log
import ro.greg.swapestate.core.Constants.TAG

class Utils {
    companion object {
        fun printError(message: String) {
            Log.d(TAG, message)
        }
    }
}